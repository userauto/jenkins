/* groovylint-disable-next-line CompileStatic */
pipeline {
    agent any

    stages {
        stage('Github clone and print file index.html') {
            steps {
                sh '''
                    pwd
                    ls -la
                    cat ./site/index.html
                '''
            }
        }
    }
}
